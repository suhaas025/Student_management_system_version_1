package com.example.try2.entity;

public enum EAccountStatus {
    ACTIVE,    // Account is active and usable
    EXPIRED,   // Account has expired due to inactivity
    BLOCKED    // Account has been manually blocked by an admin
} 